coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: auto
        threshold: 5%

ignore:
  - "cmd/**"
  - "main.go"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true

